namespace Pokemon3D.DataModel.GameMode.Battle
{
    /// <summary>
    /// Used to identify the type of script hook used in a move's hook list.
    /// </summary>
    public enum MoveHookType
    {
        /// <summary>
        /// When the move gets selected from the list of moves available to a Pokémon.
        /// </summary>
        OnMoveSelection
    }
}
